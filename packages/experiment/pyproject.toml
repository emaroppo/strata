[project]
name = "strata-experiment"
version = "0.1.0"
description = "An experiment written down once: stages from a config, a grid over them, and a ledger of what ran"
requires-python = ">=3.13"
# The top of the graph. It sequences the other packages' stages, so it
# depends on all of them; nothing depends on it.
dependencies = [
    "pydantic>=2.0",
    "strata-labels>=0.1,<0.2",
    "strata-common>=0.1,<0.2",
    "strata-catalog>=0.1,<0.2",
    "strata-modelling>=0.1,<0.2",
    "strata-labeller>=0.1,<0.2",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[project.scripts]
strata-experiment = "strata.experiment.cli:main"

[tool.uv.sources]
strata-labels = { workspace = true }
strata-common = { workspace = true }
strata-catalog = { workspace = true }
strata-modelling = { workspace = true }
strata-labeller = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# PEP 420 namespace: no strata/__init__.py in any package, or they stop merging
packages = ["strata"]
